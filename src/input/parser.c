#include "../../inc/ping.h"

// static void minimized_parser(t_config * config, char * arg) {

//     (void)config;
//     (void)arg;
// }

void parser(t_config * config, char ** tokens) {

    (void)config;
    (void)tokens;
}
